framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - 'instances/'
            - 'assets/'

        excluded_patterns:
            - '*/templates/*'
            - '*/translations/*'
            - '*/config/*'